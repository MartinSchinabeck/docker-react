language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t martinschinabeck/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true martinschinabeck/docker-react npm run test 

